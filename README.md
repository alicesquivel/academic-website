# academic-website

This repository contains a personal academic website built with React + Vite and Tailwind.

Summary
- Starter: Vite + React
- Styling: Tailwind CSS
- Dev server: Vite

Recovery & backup notes
- This repository was recreated and pushed from a local copy on 2025-11-29.
- Backup branches created during recovery:
  - `recovery/20251129-161545` — original local HEAD saved and pushed
  - `main-backup-20251129-161812` — backup of prior `main` created and pushed

Getting started (local)
1. Install dependencies:
	- Using Yarn (recommended): `yarn install`
	- Or npm: `npm install`
2. Run development server:
	- `yarn dev` (or `npm run dev`) — opens at `http://localhost:5173` (or next available port)
3. Build for production:
	- `yarn build` (or `npm run build`)
4. Preview build:
	- `yarn preview` (or `npm run preview`)

Notes
- The repository now tracks the main project files (`src/`, `public/`, config files).
- A `.gitignore` was added to exclude OS/editor temporary files and dependency artifacts.

If something looks off (missing files or large binaries), check the backup branches listed above before making destructive changes.

Contributing / pushing
- Remote: `https://github.com/alicesquivel/academic-website.git`
- To push local changes:
  - `git add . && git commit -m "your message" && git push origin main`

Contact
- If you need help restoring other branches or cleaning large files, open an issue in this repo or contact the repository owner.

---
Generated/updated on 2025-11-29 by recovery script.
